# simple hyprland dots
simple dots for hyprland, contains only waybar config, hypr config and wallpapers ( u need to install hyprland, network-manager-applet, wofi, mako, slurp for screenshots(slurp is optional if u need screenshots) )
